import 'package:flutter_test/flutter_test.dart';

main() {
  group('appearance section', () {
    group('"Light mode" widget', () {
      // testWidgets(
      //     'displays check_circle icon when SharedPreferencesService.getThemeMode() is ThemeMode.light',
      //     () {});
    });
  });
}
